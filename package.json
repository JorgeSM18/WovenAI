{
  "name": "woven",
  "version": "0.0.0",
  "private": true,
  "description": "Woven — digital wardrobe with AI (monorepo root)",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "gen:types": "turbo run gen:types"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  }
}
